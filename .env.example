VITE_DATA_PROVIDER=local
# Supabase (public keys only - never expose the service_role key in the frontend)
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
VITE_EMAILJS_SERVICE_ID=
VITE_EMAILJS_TEMPLATE_ID=
VITE_EMAILJS_PUBLIC_KEY=
VITE_CONTACT_WHATSAPP=

# =============================================================================
# SERVER-SIDE / LOCAL-SCRIPT ONLY (never VITE_-prefixed, never bundled)
# Used by `npm run seed:supabase` (scripts/seed-supabase-demo-users.mjs).
# Keep these in .env.local (git-ignored) or the process environment only.
# =============================================================================
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=